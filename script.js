const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.main-nav');if(toggle&&nav){toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open));toggle.textContent=open?'Fechar':'Menu'});nav.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{nav.classList.remove('open');toggle.setAttribute('aria-expanded','false');toggle.textContent='Menu'}))}const carousel=document.querySelector('[data-carousel]');if(carousel){const quotes=[...carousel.querySelectorAll('.quote')];const status=carousel.querySelector('[data-status]');let current=0;const show=(index)=>{current=(index+quotes.length)%quotes.length;quotes.forEach((quote,i)=>quote.classList.toggle('active',i===current));status.textContent=`0${current+1} / 0${quotes.length}`};carousel.querySelector('[data-prev]').addEventListener('click',()=>show(current-1));carousel.querySelector('[data-next]').addEventListener('click',()=>show(current+1))}
